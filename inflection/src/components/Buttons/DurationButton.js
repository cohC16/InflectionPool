import React, { Component } from "react";

export default class DurationButton extends Component {
  constructor(props) {
    super(props);
  }
  render() {
    return <p>DurationButton component</p>;
  }
}
