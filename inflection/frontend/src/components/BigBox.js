import React, { Component } from "react";

// import "./linegraph.css";
import {
  LineChart,
  ResponsiveContainer,
  Legend,
  Tooltip,
  Line,
  XAxis,
  YAxis,
  CartesianGrid,
  Label,
} from "recharts";

function BigBoxComponent() {
  return <ResponsiveContainer width="85%" aspect={3} />;
}
export default BigBoxComponent;
