import React, { Component } from "react";

export default class InteractionDisplay extends Component {
  constructor(props) {
    super(props);
  }
  render() {
    return <p>InteractionDisplay component</p>;
  }
}
